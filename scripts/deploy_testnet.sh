#!/usr/bin/env bash
# veil-kyc: deploy the verifier, policy_gate, and stablecoin_demo contracts to
# Stellar testnet.
#
# Day 1: stub only. Full deployment lands with the compiled contracts
# (Days 3-5). Reference command from README "Getting Started":
#
#   soroban contract deploy \
#     --wasm contracts/verifier/target/wasm32-unknown-unknown/release/verifier.wasm \
#     --network testnet
set -euo pipefail

echo "veil-kyc deploy_testnet.sh: not implemented yet (Days 3-5)." >&2
exit 0
